# 2026-08-10

## 家长需求周更新（自动化 automation-1786041927191）首次执行
- 重写 `assets/data-parent-weekly.js` 覆盖层，季节 summer_bridge，落地 7 条本周家长焦点（分班考开考期主题）。
- 未触碰任何基础库文件；`node --check` 通过；CloudStudio 重新部署成功（链接不变）；git commit `14f39c5` 已推送 GitHub。
- 周报邮件已发至 315675111@qq.com。

## 竞品雷达周更（自动化 automation-1786042813796）首次执行
- 重写 `assets/data-rival.js`：accounts 68→71、institutions 48→54，meta.asOf=2026-08-10，其余字段（teacherMoves/zhaosheng2026/marketGaps）完整保留；`node --check` 通过。
- 新增账号：月照春山_语文提分版（山西，117.9万，单月净涨约37万）、CDC舞蹈艺考（太原，50.1万）、890舞蹈艺考（太原，39.1万）。
- 新增机构：太原高途、890舞蹈、CDC舞蹈艺考、太原蔚来国际教育、山西登科教育、众学稳尚；并把太原锐思校区数由 10 更正为 14。
- git commit `d197dc0` 已推送 GitHub；**CloudStudio 部署 3 次全失败（400 / fetch failed），线上链接仍是旧数据，需手动重部署**。
- 周报 + 一封 GitHub 状态更正邮件已发至 315675111@qq.com。

## 工程备忘
- data-calendar.js 的全局变量是 `window.SCHOOL_CAL`（不是 CAL），hot-events.js 是 `window.HOT`。后续脚本引用需注意。
- agent-mail 发信为两步：首次返回 CONFIRMATION_REQUIRED + token，需带 token 重发。**重发时 body 必须与首次完全一致，改一个字符就报 40001。**
- `git push` 报 "Recv failure: Operation timed out" 不代表真失败，用 `git ls-remote origin main` 复核远端 ref 再下结论。
- 竞品检索最有效入口：飞瓜地区榜 `dy3.feigua.cn/rank/area/4/48/day/YYYYMMDD.html`（太原市）、`/area/4/0/`（山西省）、`/rank/growing/13/`（教育校园行业榜），能直接拿到真实账号名+粉丝数+增量。
- REDFOX_API_KEY 未配置，douyin-search / xiaohongshu-search 技能不可用，竞品类任务目前只能靠 WebSearch。
