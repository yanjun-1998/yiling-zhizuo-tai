# automation-1786038038517 · 周日对标迭代 执行记录

> 仅记录高层执行摘要，不放正文内容。完整周报见 `周迭代/YYYY-WW.md`。

---

## 2026-08-09（2026-W32）· 首次执行 · ✅ 成功

- **对标**：王芳英语 / 陈进步讲数学 / 高中物理王哲 / 太原升学直播生态 → 检索到公开信号；张毅物理、张强数学 → 未检索到，已在周报标注不采信。benchmark 5 条。
- **落地**：覆盖层 21 条键（physics 6 / eduplan 5 / politics 3 / live 5 / tier1 2 / moment 0）。
- **自检**：`node --check` 通过；`tools/qc.js` 通过 92/92，修正 0 轮；额外写了临时脚本校验覆盖层键命中 → 21/21 全命中。
- **部署**：CloudStudio ✅（链接未变，`1e82f75555f54f10877a320bdd75aba9.gz2.agentos-app.net`，密码 324）；GitHub 覆盖层 ✅ `269bff1`；周报 commit `bca97af` ⚠️ push 网络超时未推成功，需后续补 push。
- **邮件**：✅ 已发至 315675111@qq.com。

### 本次踩到的坑 / 下次直接沿用的经验

1. **`tools/qc.js` 只校验基础数据，不校验覆盖层键是否写错**。键写错 = 覆盖静默失效，是本机制唯一隐性失败模式。本次靠临时脚本 `/tmp/verify_override.js`（把 ITER_OVERRIDE 与基础 data 做 join）补位。**建议尽早固化进 qc.js**，在此之前每周都要手动补这一步。
2. **eduplan 里有强时效条目**，如 `第三次补报志愿正在进行（8/5-8/7）`——标题写死了日期，过期后仍在库里。本次靠 hook/solution 层做纠偏（改为提示职高补报开到 9/20）。**每周执行时务必先核对 eduplan 时效条目的真实状态**。
3. 取键必须逐字复制基础数据原文，含中英文标点与全角引号；physics 有条目标题自带中文引号 `"..."`，写 JS 时用单引号包裹避免转义错误。
4. `timeout` 命令在本机 zsh 不可用，别用。
5. agent-mail SendMessage 首次调用会返回 CONFIRMATION_REQUIRED + token，需带 token 重发一次；本任务已获主理人常设授权（"无需审核"），可直接确认。
6. GitHub push 偶发网络超时，重试 3 次仍可能失败。**失败时不得声称已上线**，如实写入周报与邮件；CloudStudio 与 GitHub 是两条独立通道，一条失败不影响另一条。
